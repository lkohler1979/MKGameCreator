// Compartilhado entre o motor do jogo (platform-game.ts) e a montagem do
// sceneConfig a partir do desenho (scene-config-builder.ts) — evita os dois
// lados divergirem sobre o tamanho do "mundo" do nível.
export const LEVEL_WIDTH = 1000;
export const LEVEL_HEIGHT = 560;
export const GROUND_Y = 500;
export const FLAG_X = 900;
